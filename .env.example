# === Supabase（選填）===
# 不填的話，App 會自動改用瀏覽器 localStorage，資料只存在你這台裝置、無法多人共享。
# 填入後即可多人共用同一份資料（大家看到同一個揪團）。
# 到 https://supabase.com 免費建立專案後，於 Project Settings → API 取得：
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# === AI 彙整建議（選填）===
# AI 功能透過 Supabase Edge Function 呼叫 Claude，金鑰放在後端（見 supabase/functions/ai-suggest）。
# 這裡不需要填 Claude 金鑰；金鑰用 `supabase secrets set ANTHROPIC_API_KEY=...` 設在後端。
# 若你的 Edge Function 名稱或網址不同，可在此覆寫（預設用上面的 VITE_SUPABASE_URL 組出來）：
VITE_AI_FUNCTION_URL=
